import{api,money,pretty}from'@/lib/api';export default async function Dashboard(){const s=await api.stats();return <main className="shell section"><p className="eyebrow">Portfolio view</p><h1 style={{fontSize:36}}>Semiconductor project dashboard</h1><div className="grid"><div className="stat"><strong>{money(s.total_announced_investment_inr)}</strong><span>Disclosed investment</span></div><div className="stat"><strong>{s.total_projects}</strong><span>Published projects</span></div><div className="stat"><strong>{s.states}</strong><span>States</span></div></div><section className="section"><h2>Projects by status</h2><div className="grid">{s.by_status.map(x=><div className="card" key={x.label}><strong style={{fontSize:28}}>{x.count}</strong><br/><span className="muted">{pretty(x.label)}</span></div>)}</div></section></main>}
