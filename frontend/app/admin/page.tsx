'use client';import{useState}from'react';export default function Admin(){const[key,setKey]=useState('');const[message,setMessage]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body={project_name:f.get('project_name'),company_name:f.get('company_name'),state:f.get('state'),project_type:f.get('project_type'),status:f.get('status'),sources:[{source_type:'Other',publisher:f.get('publisher'),title:f.get('title'),url:f.get('url'),reliability:'MEDIUM'}]};const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000'}/api/admin/projects`,{method:'POST',headers:{'Content-Type':'application/json','X-Admin-Key':key},body:JSON.stringify(body)});setMessage(r.ok?'Draft project created. Add verification details before publishing.':`Could not save: ${await r.text()}`)}return <main className="shell section"><p className="eyebrow">Admin workflow</p><h1 style={{fontSize:36}}>Create a sourced project draft</h1><form className="card" onSubmit={submit} style={{maxWidth:700,display:'grid',gap:12}}><input type="password" placeholder="Admin API key" value={key} onChange={e=>setKey(e.target.value)} required/><input name="project_name" placeholder="Project name" required/><input name="company_name" placeholder="Company" required/><input name="state" placeholder="State" required/><select name="project_type"><option>Semiconductor Fab</option><option>OSAT</option><option>ATMP</option><option>Semiconductor Packaging</option></select><select name="status"><option>ANNOUNCED</option><option>APPROVED</option><option>UNDER_CONSTRUCTION</option></select><hr/><b>Required evidence</b><input name="publisher" placeholder="Publisher" required/><input name="title" placeholder="Source title" required/><input name="url" type="url" placeholder="https://source.example" required/><button className="button">Save draft</button>{message&&<span>{message}</span>}</form></main>}
