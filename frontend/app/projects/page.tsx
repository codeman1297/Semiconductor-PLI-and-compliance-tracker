import{api,pretty}from'@/lib/api';import{ProjectTable}from'@/components/project-table';export default async function Projects({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const s=await searchParams;const params=new URLSearchParams();for(const k of ['q','status','state','project_type','sort'])if(s[k])params.set(k,s[k]!);const data=await api.projects(`?${params}`);return <main className="shell section"><p className="eyebrow">Database</p><h1 style={{fontSize:36}}>India semiconductor projects</h1><form className="filters"><input name="q" defaultValue={s.q} placeholder="Search project, company, state"/><select name="status" defaultValue={s.status}><option value="">All statuses</option>{['APPROVED','UNDER_CONSTRUCTION','OPERATIONAL','DELAYED'].map(x=><option key={x}>{x}</option>)}</select><select name="state" defaultValue={s.state}><option value="">All states</option><option>Gujarat</option><option>Assam</option></select><button className="button">Apply filters</button></form><p className="muted">{data.total} source-backed published records.</p><ProjectTable items={data.items}/></main>}
