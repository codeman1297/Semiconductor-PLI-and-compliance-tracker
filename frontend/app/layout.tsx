import './globals.css';import{Layout}from'@/components/layout';import type{Metadata}from'next';export const metadata:Metadata={title:'India Semiconductor Project Tracker',description:'Source-backed database tracking semiconductor fabs, OSAT/ATMP, packaging and major investments across India.',metadataBase:new URL('http://localhost:3000')};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><Layout>{children}</Layout></body></html>}
